/*include <Obstaculo.hpp>

namespace Entidades{
    namespace Obstaculos{
        class Portal: public Obstaculo{
            private: 
                bool mortal;
            public:
                Portal(const sf::Vector2f tamanho, const sf::Vector2f posicao);
                ~Portal();
                bool getMortal();
                void executar();
                void inicializar();
                void Obstacular(Entidades::Entidade* pJogador);//sei que é jogador por causa da condição no gerenciador de colisões
        };
    }
}*/